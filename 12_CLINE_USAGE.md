# 12 — HOW TO RUN WITH CLINE

Open this folder as the VS Code workspace so `.clinerules` is active.

Use Cline in Act mode and send exactly:

> Read `10_CLINE_MASTER_PROMPT.md` and execute it autonomously to completion. The Blueprint is pre-approved. Do not stop for intermediate approval unless you encounter a Level-3 blocker.

Then let Cline run through:
- source scan
- external asset sourcing
- build
- browser QA
- refinement
- final report
